spring:
  webflux:
    static-locations: "classpath:/static/"
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods:
              - GET
              - OPTIONS
              - POST
server:
  port: 3002
